{
  "name": "bilisops-chat",
  "version": "0.1.0",
  "private": true,
  "description": "BilisOps Chat — multichannel seller inbox (React frontend + Express/Claude backend)",
  "main": "server.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "server": "node server.js",
    "start": "npm run build && node server.js",
    "cf:dev": "npm run build && wrangler dev",
    "cf:deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@supabase/supabase-js": "^2.45.4",
    "express": "^4.21.0",
    "hono": "^4.6.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11",
    "wrangler": "^4.110.0"
  }
}
